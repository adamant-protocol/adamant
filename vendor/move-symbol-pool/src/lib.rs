//! **Placeholder.** Vendored Sui code lands in a follow-up commit
//! per `vendor/README.md`. This file exists so the workspace
//! builds cleanly during the scaffold stage.
//!
//! See `PROVENANCE.md` for the upstream tag, audit posture, and
//! the placeholder note on this crate's inherited upstream
//! `unsafe` surface (string-interning pattern). The actual
//! `move-symbol-pool` upstream source from
//! `external-crates/move/crates/move-symbol-pool` at Sui release
//! tag `mainnet-v1.66.2` is copied in by the follow-up vendor
//! commit (Batch 2 of the Sui-Move vendoring).
